r = _T('\0');

extern 
                 "C"; //Übersprungen or not - Questionmark

#include <windows.h> //Is this Line Skipped - Questionmark

#                       \
\
\
\
\
        include "TesttheTest.h"

TCHAR szString[] = _T("Hello, World"); /*This is a comment over
                                                                  many lines
                                                                  ore
                                                                  not - Questionmark*/

void near void(nichts)
        {
        TCHAR *p = strrchar(szString,_T('.'));

        MessageBox(_T("Hello ihr Arschlöcher !"));

        TCHAR szOverManyString[] = _T("Wenn die Hühner fligen\n")\
                                     _T("Dann Fliegen Sie")

        MessageBox(_T("Dies ist ein \"String\" in einem String")));

         if(!Reg.SetValue(NULL,_T("[open(\"%1\"]"),13L))
                return;

        if(*p == _T('h'))
                goto Scheiße _T("Ornot")_T("Xxx");

        r = _T("\\");
        b = TEXT ( "\\" );

	strtok(_T("Test"),_T("e"));

        /*This Keyword is in a comment char or not */

        //and this char, too

                L"Unicode Sau";

                L'i';




                szBuf = _T("Codes");

                __T("Next Unicode Sau") __TEXT("Offset to the Unicode-Sau");

        UNICODE _TEXT   \
        ("hello Unicode-Sau");

        szBuf = _T("Windows XP ist eine Unicode-Sau");

        //Ob dieses Hexadezimale Element umgewandelt wird??

        /*









        */


        _istxdigit

        return nichts;
